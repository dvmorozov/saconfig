@model SAConfig.Models.saconfig_tConnectedAP

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Details</h2>

<fieldset>
    <legend>ConnectedAP Attributes</legend>

    <div class="display-label">desc</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.desc)
    </div>

    <div class="display-label">iedName</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.iedName)
    </div>

    <div class="display-label">apName</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.apName)
    </div>

    <div class="display-label">SubNetwork</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.saconfig_tSubNetwork.desc)
    </div>

</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
