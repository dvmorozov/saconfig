@model IEnumerable<SAConfig.Models.saconfig_tLN>

@{
    ViewBag.Title = "LN";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>LN</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            desc
        </th>
        <th>
            lnType
        </th>
        <th>
            prefix
        </th>
        <th>
            lnClass
        </th>
        <th>
            inst
        </th>
        <th>
            LDevice
        </th>
        <th>
            AccessPoint
        </th>
        <th>
            Owner Type
        </th>
        <th>
            DataOwnerID
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.lnType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.prefix)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.saconfig_tLNClassEnum.value)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.inst)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.saconfig_tLDevice.desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.saconfig_tAccessPoint.desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.saconfig_LNOwnerType.OwnerType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataOwnerID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
