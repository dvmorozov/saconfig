@model SAConfig.Models.saconfig_tConnectivityNode

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Details</h2>

<fieldset>
    <legend>ConnectivityNode Attributes</legend>

    <div class="display-label">pathName</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.pathName)
    </div>

    <div class="display-label">desc</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.desc)
    </div>

    <div class="display-label">name</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.name)
    </div>

    <div class="display-label">Bay</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.saconfig_tBay.desc)
    </div>

</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
