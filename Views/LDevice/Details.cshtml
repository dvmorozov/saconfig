@model SAConfig.Models.saconfig_tLDevice

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Details</h2>

<fieldset>
    <legend>LDevice Attributes</legend>

    <div class="display-label">desc</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.desc)
    </div>

    <div class="display-label">inst</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.inst)
    </div>

    <div class="display-label">ldName</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.ldName)
    </div>

    <div class="display-label">Server</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.saconfig_tServer.desc)
    </div>

</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
