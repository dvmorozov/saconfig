@model SAConfig.Models.saconfig_tSubstation

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

<p>
    @Html.ActionLink("VoltageLevel", "Index", "VoltageLevel") |
    @Html.ActionLink("Function", "Index", "Function") |
    @Html.ActionLink("GeneralEquipment", "Index", "GeneralEquipment") |
    @Html.ActionLink("PowerTransformer", "Index", "PowerTransformer") |
    @Html.ActionLink("LNode", "Index", "LNode")
</p>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Substation Attributes</legend>

        @Html.HiddenFor(model => model.ID)
        @Html.Hidden("sclID", (long)ViewBag.SCLID)

        <div class="editor-label">
            @Html.LabelFor(model => model.desc)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.desc)
            @Html.ValidationMessageFor(model => model.desc)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.name)
            @Html.ValidationMessageFor(model => model.name)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    <a href="@Html.Raw((String)(ViewBag.BackURL))">Back to Upper Element</a>
</div>
