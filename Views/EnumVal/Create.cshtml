@model SAConfig.Models.saconfig_tEnumVal

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>EnumVal Attributes</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.ord)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ord)
            @Html.ValidationMessageFor(model => model.ord)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.EnumType, "EnumType")
        </div>
        <div class="editor-field">
            @Html.DropDownList("EnumType", String.Empty)
            @Html.ValidationMessageFor(model => model.EnumType)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
