@model SAConfig.Models.saconfig_tSettingControl

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>SettingControl Attributes</legend>

    <div class="display-label">desc</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.desc)
    </div>

    <div class="display-label">actSG</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.actSG)
    </div>

    <div class="display-label">numOfSGs</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.numOfSGs)
    </div>

    <div class="display-label">LN0</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.saconfig_tLN0.desc)
    </div>

</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
