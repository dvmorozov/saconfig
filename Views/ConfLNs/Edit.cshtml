@model SAConfig.Models.saconfig_tConfLNs

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>ConfLNs Attributes</legend>

        @Html.HiddenFor(model => model.ID)

        <div class="editor-label">
            @Html.LabelFor(model => model.fixPrefix)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.fixPrefix)
            @Html.ValidationMessageFor(model => model.fixPrefix)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.fixLnInst)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.fixLnInst)
            @Html.ValidationMessageFor(model => model.fixLnInst)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Services, "Services")
        </div>
        <div class="editor-field">
            @Html.DropDownList("Services", String.Empty)
            @Html.ValidationMessageFor(model => model.Services)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
