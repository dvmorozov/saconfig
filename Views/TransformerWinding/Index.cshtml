@model IEnumerable<SAConfig.Models.saconfig_tTransformerWinding>

@{
    ViewBag.Title = "TransformerWinding";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>TransformerWinding</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            type
        </th>
        <th>
            virtual
        </th>
        <th>
            PowerTransformer
        </th>
        <th>
            DataOwnerID
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.virtual_)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.saconfig_tPowerTransformer.type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataOwnerID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
