@model IEnumerable<SAConfig.Models.saconfig_tGSE>

@{
    ViewBag.Title = "GSE";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>GSE</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            desc
        </th>
        <th>
            ldInst
        </th>
        <th>
            cbName
        </th>
        <th>
            ConnectedAP
        </th>
        <th>
            DataOwnerID
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ldInst)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cbName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.saconfig_tConnectedAP.desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataOwnerID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
