@model IEnumerable<SAConfig.Models.saconfig_tVal>

@{
    ViewBag.Title = "Val";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Val</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            sGroup
        </th>
        <th>
            DAI
        </th>
        <th>
            DA
        </th>
        <th>
            BDA
        </th>
        <th>
            Owner Type
        </th>
        <th>
            DataOwnerID
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.sGroup)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.saconfig_tDAI.desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.saconfig_tDA.desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.saconfig_tBDA.desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.saconfig_ValOwnerType.OwnerType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataOwnerID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
