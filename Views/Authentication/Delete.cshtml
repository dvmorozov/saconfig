@model SAConfig.Models.saconfig_Authentication

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Authentication Attributes</legend>

    <div class="display-label">none</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.none)
    </div>

    <div class="display-label">password</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.password)
    </div>

    <div class="display-label">weak</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.weak)
    </div>

    <div class="display-label">strong</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.strong)
    </div>

    <div class="display-label">certificate</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.certificate)
    </div>

    <div class="display-label">Server</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.saconfig_tServer.desc)
    </div>

</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
