@model SAConfig.Models.saconfig_tTapChanger

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Details</h2>

<fieldset>
    <legend>TapChanger Attributes</legend>

    <div class="display-label">type</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.type)
    </div>

    <div class="display-label">virtual</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.virtual_)
    </div>

    <div class="display-label">TransformerWinding</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.saconfig_tTransformerWinding.type)
    </div>

</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
