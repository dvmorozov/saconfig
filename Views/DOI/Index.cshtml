@model IEnumerable<SAConfig.Models.saconfig_tDOI>

@{
    ViewBag.Title = "DOI";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>DOI</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            desc
        </th>
        <th>
            name
        </th>
        <th>
            ix
        </th>
        <th>
            accessControl
        </th>
        <th>
            LN
        </th>
        <th>
            LN0
        </th>
        <th>
            Owner Type
        </th>
        <th>
            DataOwnerID
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ix)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.accessControl)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.saconfig_tLN.desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.saconfig_tLN0.desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.saconfig_DOIOwnerType.OwnerType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataOwnerID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
